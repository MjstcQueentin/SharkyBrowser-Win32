<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SharkyBrowser.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SharkyBrowser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt"/>
    </Window.SystemBackdrop>

    <TabView VerticalAlignment="Stretch" x:Name="TabControl" AddTabButtonClick="TabView_AddTabButtonClick" TabCloseRequested="TabControl_TabCloseRequested">
        <TabView.Resources>
            <ResourceDictionary>
                <StaticResource x:Key="TabViewItemHeaderBackgroundSelected" ResourceKey="LayerOnMicaBaseAltFillColorDefaultBrush"></StaticResource>
            </ResourceDictionary>
        </TabView.Resources>

        <TabView.TabStripHeader>
            <Grid x:Name="ShellTitlebarInset"  Background="Transparent" />
        </TabView.TabStripHeader>
        <TabView.TabStripFooter>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="150" />
                </Grid.ColumnDefinitions>

                <Grid x:Name="CustomDragRegion" Grid.Column="0" Background="Transparent" />
                <Button Grid.Column="1" Style="{StaticResource CommandBarFlyoutEllipsisButtonStyle}">
                    <Button.Content>
                        <FontIcon FontSize="16" Glyph="&#xE712;" />
                    </Button.Content>
                    <Button.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem x:Name="PopLastClosedTabMenuFlyoutItem" x:Uid="/MainWindowResources/PopLastClosedTabMenuFlyoutItem" Click="PopLastClosedTabMenuFlyoutItem_Click" IsEnabled="False">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE78B;"/>
                                </MenuFlyoutItem.Icon>
                                <MenuFlyoutItem.KeyboardAccelerators>
                                    <KeyboardAccelerator Modifiers="Control,Shift" Key="T" />
                                </MenuFlyoutItem.KeyboardAccelerators>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>
                <Grid Grid.Column="2"></Grid>
            </Grid>
        </TabView.TabStripFooter>
    </TabView>
</Window>
